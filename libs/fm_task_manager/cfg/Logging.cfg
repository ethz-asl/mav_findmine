#! /usr/bin/env python
# Gimbal task configuration

PACKAGE='fm_task_manager'

from task_manager_lib.parameter_generator import *

gen = TaskParameterGenerator()
#       Name                    Type            Description
#       Default    Min   Max
gen.add("directory", str_t, 0,  "The directory where the bag should be stored.",  "/tmp")
gen.add("record_cam", bool_t, 0, "Activate camera logging.", False)

exit(gen.generate(PACKAGE, "task_server", "Logging"))
