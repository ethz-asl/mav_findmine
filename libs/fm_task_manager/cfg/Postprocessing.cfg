#! /usr/bin/env python
# Postprocessing configuration

PACKAGE='fm_task_manager'

from task_manager_lib.parameter_generator import *

gen = TaskParameterGenerator()
#       Name                    Type            Description
#       Default    Min   Max
gen.add("topics", str_t, 0,  "A comma seperated list of topics to export.",  "a1,a2,a3,a4")
gen.add("directory", str_t, 0,  "The directory where the solution should be stored.",  "/tmp")

exit(gen.generate(PACKAGE, "task_server", "Postprocessing"))
