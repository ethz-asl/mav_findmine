#! /usr/bin/env python
# Generate single trajectory task configuration

PACKAGE='fm_task_manager'

from task_manager_lib.parameter_generator import *

gen = TaskParameterGenerator()

# Common.
gen.add("altitude", double_t, 0, "The altitude above take off in m.", 5.)
gen.add("mission_file", str_t, 0, "The mission file.",  "/tmp/mission.yaml")
gen.add("plan_from_home", bool_t, 0, "Planning from home position", False)

exit(gen.generate(PACKAGE, "task_server", "GenerateSingleTrajectory"))
